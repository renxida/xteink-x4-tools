# Generated XTC files (multi-GB)
samples/
# Private SD backup
sdcard-backup/
# Snapshots of upstream code (linked instead)
refs/
# The calibre-xtc fork lives in its own repo
__pycache__/
*.pyc

# Don't track local working copy of fork — it's a submodule
